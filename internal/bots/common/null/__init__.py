#import os
#import requests


class bot:
    def __init__(self):
        self.name = 'NULL bot'
        return
    def run_bot(self, request, vntid, token, url):
        #print("null bot is running")
        #
        #print("message for process=", request)
        return
